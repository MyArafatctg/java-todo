spring:
  application:
    name: todo
  datasource:
    url: jdbc:mysql://localhost:3306/todo?allowPublicKeyRetrieval=true&useSSL=false
    username: todo
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect